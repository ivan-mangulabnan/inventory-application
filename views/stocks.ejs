<div class="stocks">
  <% if (data.length <= 0) { %>
      <h2>No Stocks Yet</h2>
  <% } else { %>
      <ul>
    <% data.forEach(stock => { %>
        <li>
          <p><%= stock.name %></p>
          <p><%= stock.quantity %></p>
          <a href="/stocks/edit/<%= stock.product_id %>">EDIT</a>
        </li>
    <% }) %>
    </ul>
  <% } %>
  <a href="/stocks/add">INCLUDE PRODUCT</a>
</div>