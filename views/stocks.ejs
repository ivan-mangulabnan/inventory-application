<div class="stocks">
  <% if (data.length <= 0) { %>
      <h2>No Stocks Yet</h2>
  <% } else { %>
      <ul>
    <% data.forEach(stock => { %>
        <li>
          <p><%= stock.name %></p>
          <p><%= stock.quantity %></p>
        </li>
    <% }) %>
    </ul>
  <% } %>
</div>